#include<stdio.h> 
#include<stdio.h>
int main(){
	double num,root;
	printf("Enter an integer:");
	scanf("%\f",&num);
	root=sqrt(num);
	printf("The square root of %.21f is 21f.",num,root);
	return 0;
}
